#include "main.h"
/**
 * print_alphabet - Prints  alphabets in lowecase.
 *
 * Return: Always return 0 (Success)
 */
void print_alphabet(void)
	void print_alphabet(void)
{
	char la;

	for (la  = 'a'; la <= 'z'; la++)
		_putchar(la);

	_putchar('\n');
}
